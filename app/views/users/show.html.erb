<h1 align="center">Olá, <%= @user.username %>!!</h1>
<div class="row">
    <div class="col-md-4 col-md-offset-4 center">
        <%= gravatar_for @user, size: 150 %>
    </div>
</div>

<h4 align="center">Esse são seus artigos, <%= @user.username %></h4>

<div class="center">
    <%= will_paginate @user_articles, :previous_label => 'Próximo', :next_label => 'Anterior' %>
</div>

<%= render 'articles/article', obj: @user_articles %>

<div class="center">
    <%= will_paginate @user_articles, :previous_label => 'Próximo', :next_label => 'Anterior' %>
</div>