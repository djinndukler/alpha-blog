<h1 align="center">Olá, <%= @users.username %>!!</h1>
<div class="row">
    <div class="col-md-4 col-md-offset-4 center">
        <%= gravatar_for @users, size: 150 %>
    </div>
</div>

<h4 align="center">Esse são seus artigos, <%= @users.username %></h4>

<div class="center">
    <%= will_paginate @users_articles, :previous_label => 'Próximo', :next_label => 'Anterior' %>
</div>

<%= render 'articles/article', obj: @users_articles %>

<div class="center">
    <%= will_paginate @users_articles, :previous_label => 'Próximo', :next_label => 'Anterior' %>
</div>